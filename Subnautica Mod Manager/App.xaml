<Application x:Class="SubnauticaModManager.SubnauticaModManager.CommonUtils.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SubnauticaModManager"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--
        <Style TargetType="Control" x:Key="BaseStyle">
            <Setter Property="Background" Value="#292929"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="ListBox" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="ListView" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="ListViewItem" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="Window" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="DockPanel">
            <Setter Property="Background" Value="#292929"/>
        </Style>
            -->
    </Application.Resources>
</Application>
